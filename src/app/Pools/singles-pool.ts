export class SinglesPool {
  id: number;
  roundId: number;
  number: number;

  constructor(roundId: number, number: number) {
      this.id = null;
      this.roundId = roundId;
      this.number = number;
  }
}
